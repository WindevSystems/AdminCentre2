<%= render 'dashboards/navbar' %>

<div class="container mt-4">
    <div class="row justify-content-center"> <!-- BEGIN: Center the row -->
        <div class="col-md-6">
            <div class="card">
                <div class="card-body">
                    <div class="d-flex align-items-center">
                        <img src="<%= @admin.avatar_url %>" alt="Admin Avatar" class="rounded-circle mr-3" width="256">
                        <div style="margin-left:20px;"></div>
                        <div>
                            <h1><%= @admin.full_name %></h1>
                            <p><%= consolidate_roles(@admin) %><br /><%= @admin.email %></p>
                            <% # Development Placeholders: take a lyric from Hawaii Pt II %>
                            <h4>About Me</h4>
                            <p><%= @admin.bio %></p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>